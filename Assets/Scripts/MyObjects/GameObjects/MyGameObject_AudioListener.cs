using System;
using System.Collections.Generic;
using UnityEngine;

[RequireComponent(typeof(AudioListener))]
public class MyGameObject_AudioListener : MyGameObject
{
    public override string id => "AudioListener";
}